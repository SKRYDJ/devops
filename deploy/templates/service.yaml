apiVersion: v1
kind: Service
metadata:
  name: {{ include "prometheus-network-check.fullname" . }}
  labels:
    {{- include "prometheus-network-check.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "prometheus-network-check.selectorLabels" . | nindent 4 }}
