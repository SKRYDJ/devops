apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "prometheus-network-check.fullname" . }}-test-connection"
  labels:
    {{- include "prometheus-network-check.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "prometheus-network-check.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
